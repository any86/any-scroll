/**
 * event(Mouse|Touch) => BasicsInput => Input => Computed => AnyTouchEvent
 * 构造统一的Input格式
 */
import { Input } from '@any-touch/shared';
export default function (): (basicsInput: any) => Input | void;
